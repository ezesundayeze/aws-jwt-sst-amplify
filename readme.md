# Using JWT to Authenticate Serverless Applications Built on SST

## Installation

- Clone the repository and run:

```shell
npm install
```

- Configure `aws-exports.js` with `aws-exports.example`

## Run

```shell
npm start
```